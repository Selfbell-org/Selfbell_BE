package com.selfbell.sos.repository;

import com.selfbell.sos.domain.SosTemplate;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SosTemplateRepository extends JpaRepository<SosTemplate, Long> {
}
